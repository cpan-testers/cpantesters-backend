$summary: Routines working with MetaCPAN

log:
  $class: Beam::Wire
  file: log.yml
  $default:
    file: fallback/log.yml

db:
  $class: Beam::Wire
  file: db.yml
  $default:
    file: fallback/db.yml

fetch_uploads:
  $class: CPAN::Testers::Backend::FetchUploads
  schema:
    $ref: db/schema

$summary: Routines working with MetaCPAN

log:
  $class: Beam::Wire
  file: log.yml

db:
  $class: Beam::Wire
  file: db.yml

fetch_uploads:
  $class: CPAN::Testers::Backend::FetchUploads
  schema:
    $ref: db/schema

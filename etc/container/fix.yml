$summary: Routines to fix data problems

common:
    $class: Beam::Wire
    file: common.yml

tester_name:
    $class: CPAN::Testers::Backend::Fix::TesterName
    schema:
        $ref: common/schema
    metabase_dbh:
        $ref: common/metabase_dbh


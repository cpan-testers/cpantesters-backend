$summary: Routines to view data

log:
  $class: Beam::Wire
  file: log.yml

db:
  $class: Beam::Wire
  file: db.yml

metabase:
  $class: CPAN::Testers::Backend::ViewMetabaseCache
  metabase_dbh:
    $ref: db/metabase_dbh
